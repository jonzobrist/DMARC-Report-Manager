# DMARC Report Manager Configuration

# Host to listen on (0.0.0.0 for all interfaces, 127.0.0.1 for localhost)
APP_HOST=127.0.0.1

# Backend Port to listen on
BACKEND_PORT=8000

# Frontend Port for dev server
FRONTEND_PORT=5173

# Allowed hosts for Backend (Host header validation)
ALLOWED_HOSTS=localhost,127.0.0.1

# Allowed origins for CORS (Browser cross-origin requests)
# Must be full URLs including protocol.
# Example: https://dmarc.zlyxy.me,http://localhost:5173
# If left empty, it will be derived from ALLOWED_HOSTS (http/https).
CORS_ALLOWED_ORIGINS=

# Secret key for JWT signing
SECRET_KEY=super-secret-key-change-me-in-production

# Database path
DB_PATH=dmarc_reports.db

# API Endpoint Configuration
# --------------------------
# The URL where the backend API can be reached.
# This is used by:
#  1. The browser/frontend (must have VITE_ prefix)
#  2. CLI tools (checks DMARC_API_URL first, then VITE_API_URL)
#
# Examples:
#   Local: http://localhost:8000
#   Remote: https://dmarc-api.zlyxy.me

VITE_API_URL=http://localhost:8000
# DMARC_API_URL=http://localhost:8000 (Optional override for CLI tools)
